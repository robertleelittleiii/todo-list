<div class="m-block" Align="Left">
    <div align="center">
    <!-- START:flash -->
    <% if notice -%>
      <div id="notice"><%= notice  %></div>
    <% end -%>
    <!-- END:flash -->
    
    <% if @todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo.errors.count, "error") %> prohibited this todo from being saved:</h2>

      <ul>
      <% @todo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    
  </div>

  <div class="m-header">Edit Todo</div>
  <div class="m-section">
    <div class="m-heading">Information for todo</div>
    <div class="m-content">

      <%= render 'form' %>

         </div>
    </p>
  </div>
  
  <%= link_to 'Back', request.env['HTTP_REFERER'] %>
   

</div>

