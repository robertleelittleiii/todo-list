<div class="m-block" Align="Left">
    <div align="center">
    <!-- START:flash -->
    <% if notice -%>
      <div id="notice"><%= notice  %></div>
    <% end -%>
    <!-- END:flash -->
    
    <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    
  </div>

  <div class="m-header">Edit User</div>
  <div class="m-section">
    <div class="m-heading">Information for user</div>
    <div class="m-content">

      <%= render 'form' %>

         </div>
    </p>
  </div>
  
<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
   

</div>

